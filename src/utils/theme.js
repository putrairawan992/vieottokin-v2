import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#0F4875'
		},
		secondary: {
			main: '#042C4B'
		},
		accent: {
			main: '#F58120'
		}
	}
});

export default theme;
