import React from 'react';
import { BrowserRouter } from 'react-router-dom';
import RouterManager from 'router';

const App = () => (
	<BrowserRouter>
		<RouterManager />
	</BrowserRouter>
);

export default App;
